import {multiply} from '../index';

describe("test multiply 1", () => {
  it('multiply 1 * 0 to equal 0', () => {
    const result = multiply(1, 0);
    expect(result).toBe(0);
  }),
  xit('multiply 1 * 0 to equal 1', () => {
    const result = multiply(1, 0);
    expect(result).toBe(1);
  })
});
describe("test multiply 2", () => {
  it('multiply 1 * 0 to equal 0', () => {
    const result = multiply(1, 0);
    expect(result).toBe(0);
  }),
  xit('multiply 1 * 0 to equal 1', () => {
    const result = multiply(1, 0);
    expect(result).toBe(1);
  })
});
